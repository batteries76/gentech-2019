# Maker Monday Challenge

Let's see what we remember...

See if you can make your own version of `.each`, `.map`, and `.select`. You can start by having your method take the array as an argument. 

**Beast**
Amend the array class to run a function called `.my_each`, etc, that do the same job as `.each`, etc.